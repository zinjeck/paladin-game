name: One-time City Work API Refactor

on:
  push:
    branches:
      - agent/extract-city-work-api

permissions:
  contents: write

jobs:
  apply-refactor:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Apply city work API extraction
        run: python3 ci/one_time_extract_city_work_api_v3.py

      - name: Commit generated refactor
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "No generated changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts ci/audit_gdscript.py
          git commit -m "Extract city work APIs from WorldData"
          git push origin "HEAD:${GITHUB_REF_NAME}"
